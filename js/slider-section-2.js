$(document).ready(function(){
    $('.slider-s2').slick({
        arrows:false,
       /*  dots:true, */
        adaptiveHeight:true,
        speed:1000,
        autoplay:false,
       /*  autoplaySpeed:1000, */
        waitForAnimate:false,
        variableWidths:true,
        infinite: true
    });
    // Доработать адаптив
});
